# Known limitations

- The Phase A runtime loop still uses SharedPreferences.
- Drift schema is present, but generated files must be created locally with build_runner.
- The app is not yet wired to SQLite at runtime.
- Sample riddles are not reviewed production Bible content.
- Optional ads, purchases, analytics, cloud sync, challenge modes, and full achievement logic are not active yet.
- Flutter/Dart commands were not run in this ChatGPT environment because Flutter is not installed here.
