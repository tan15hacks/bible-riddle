# Phase C acceptance

Phase C is acceptable when:

- content loading can run through `ContentRepository`;
- gameplay answer submission can run through `GameplayRuntime`;
- progress/economy/statistics updates go through `ProgressRepository`;
- tests cover correct answers, close answers, rewards, and hint spending;
- the existing Phase A screens remain available while UI wiring is migrated.
