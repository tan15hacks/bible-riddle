# Phase C review order

Review order:

1. Merge PR #2 first.
2. Retarget this Phase C PR from `agent/phase-b-data-layer` to `main` after Phase B is merged, if GitHub does not retarget automatically.
3. Run Flutter generation and tests locally before merging.
